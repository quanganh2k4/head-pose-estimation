# Secrets — credential camera/RTSP nằm trong .env, không bao giờ commit
.env
deployment/.env

# Logs
log/*.log
*.log

# Python cache
__pycache__/
*.py[cod]
*$py.class

# OS files
.DS_Store
Thumbs.db

# C++ Build
build/
**/build/

# TensorRT Engines
**/*.engine

# Model weights (large binaries, not suitable for git — see README for download links)
*.onnx
*.whl
models/**/*.so

# NVIDIA DeepStream SDK proprietary sample code (LicenseRef-NvidiaProprietary) —
# must NOT be redistributed; obtain locally from the DeepStream SDK install.
# See THIRD_PARTY_NOTICES.md.
models/peoplenet/custom_parser/
models/peoplenet/resnet34_peoplenet_int8.txt

.claude/
.vscode/

# Private local interview/deep-dive notes and legacy benchmark
docs/project_deep_dive_guide.md
docs/codebase_line_by_line_architecture_deep_dive.md
benchmarks/poc_monolithic_pipeline.py
